/**
 * Application property constant key to check whether an initialization
 * of the system should be done after startup.
 *
 * @type {string}
 */
// eslint-disable-next-line import/prefer-default-export
export const REQUEST_INITIALIZATION = 'REQUEST_INITIALIZATION';
